# runtime-manifest

Independently versioned deterministic metadata for component, protocol, schema,
feature, and dependency compatibility. Block 1 provides only the import/version
skeleton; Block 11 owns its descriptive behavior and schemas.

This distribution is currently unlicensed and unpublished.
